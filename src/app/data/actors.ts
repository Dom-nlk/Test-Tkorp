
import { Animal } from '../types/animal.model';
import { Person } from '../types/person.model';
import data from './data.json';

export const persons: Person[] = data.persons;
export const animals: Animal[] = data.animals;